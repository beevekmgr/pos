import React, { Component } from 'react'

export default class FinalizeSale extends Component {
  render() {
    return (
      <div>
        <button className = "btn btn-primary" id = "finalize-btn">FINALIZE THE SALE</button>
      </div>
    )
  }
}
